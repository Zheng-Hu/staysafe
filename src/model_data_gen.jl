
#n by 24 * 7 * (num_days) + 10 (for some set of potential labels)
function model_data_gen(n = 100,num_days = 10)
    ret = Array{Float64}(undef,n,24*7*num_days+10)
    for i in 1:n
        for j in 1:num_days
    end
end